apply plugin: 'com.android.application'

android {
    compileSdkVersion 19
    buildToolsVersion "19.1.0"

    defaultConfig {
        applicationId "com.flipcamera"
        minSdkVersion 14
        targetSdkVersion 19
        versionCode 1
        versionName "1.01"
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
